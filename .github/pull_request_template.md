**What changes and why**

**Numbers that change**

If any reported value changes, say which field, for which inputs, and by how
much. Add the manifest and command you used.

**Checks**

- [ ] `ruff check .`, `mypy src/music_eval` and `pytest` pass
- [ ] new behaviour has a test that fails on main
- [ ] docs updated (`docs/metrics.md`, `docs/manifest.md`, `CHANGELOG.md`) where they describe the changed behaviour
